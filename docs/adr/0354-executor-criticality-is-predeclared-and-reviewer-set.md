# 执行能力关键性须事前由方法审阅者冻结

## Status

accepted

执行能力维度是否关键，必须由人工方法审阅者在实验前根据其对候选纳入/排除、排序、风险处置、冲突解决、人工覆盖或选择机会集的潜在影响判定，并写入预注册。默认筛选规则执行、风险/隐私处置、冲突解决、人工覆盖和选择机会集为关键；事后发现影响可能时只能创建 `criticality_reclassification` revision 并进行影响分析，不能通过降级关键性恢复支持继承。
