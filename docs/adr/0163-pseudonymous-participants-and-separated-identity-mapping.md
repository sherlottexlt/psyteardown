# 参与者身份与研究结果分离保存

## Status

accepted

真人研究使用 pseudonymous participant_id，OutcomeObservation 只保存去标识 ID、分群、变体和研究条件；Identity Mapping 单独受限保存，不进入 Showcase、评测、模型输入或普通查询。逐字稿、照片和其他原始数据先脱敏；撤回同意时按协议删除/限制可识别数据，已不可回溯的去标识结果可保留但标记 consent_withdrawn/source_restricted。参与者反馈不自动写入个人 Context Pattern。
