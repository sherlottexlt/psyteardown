# 多候选采用可解释排序但不自动选择

## Status

accepted

第一版对多个设计候选执行硬约束筛选和可解释排序，依据是当前 `DesignBrief` 的目标体验、风险、证据强度、成本、隐私代价与权衡优先级；系统不输出不可解释的通用总分，也不自动选择、发布或锁定最终候选。人可以改变权重、重新排序或覆盖排序，并保存覆盖理由。这样可以减少凭直觉抽卡，同时保留设计判断和责任归属；代价是排序无法替代设计团队的最终决策。
