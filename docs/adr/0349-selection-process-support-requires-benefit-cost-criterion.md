# 选择流程支持须同时满足收益与成本判据

## Status

accepted

选择流程支持必须事前锁定结果改善指标、最小实际重要性、人工成本指标及上限/权衡规则。收益达标但成本超限只能是 `selection_process_support_with_cost` 条件性支持；联合判据未达标不能称为流程支持。成本测量同样需预注册工具、时间窗、盲法和缺失处理，且流程支持不改变候选结果证据等级。
