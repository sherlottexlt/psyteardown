# 高频结构化分支冲突触发作用域充分性审阅

## Status

accepted

高频且结构化的条件启发式冲突表明现有分支作用域可能不足，必须审阅未建模调节变量、交互、风险边界或测量差异，不能无限依赖逐案人工裁决。审阅期间相关重叠范围暂停自动注入，仅可 `validation-only` 或显式人工选择；若新调节变量得到证据支持，创建新的 Heuristic Family/branch revision，不能原地修改旧分支。
