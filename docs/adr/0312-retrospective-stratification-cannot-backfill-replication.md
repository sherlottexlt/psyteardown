# 事后分层一致性不能回填复现

## Status

accepted

调节变量经新实验确认后，旧实验中未预注册的分层结果只能标记 `retrospective_consistency`，不能回填为确认性支持单元或独立复现计数。新条件假设需新建 hypothesis/heuristic revision，并由预注册分层实验建立自身证据；旧分层结果仅用于边界说明、规划和反例搜索，冲突必须显式保留。
