# 使用三层结构化反馈连接评审与设计生成

## Status

accepted

psyteardown 对每个设计候选输出固定的三层反馈：评审状态（`blocked`、`revise` 或 `explore`）、基于证据的原因与权衡、以及指向具体设计变量的可操作修改建议。评审不能只返回抽象形容词，也不能直接替生成器决定完整方案。这样反馈可以被人阅读，也可以稳定地作为下一轮 AI 设计的输入；代价是需要维护结构化 schema 和设计变量词汇。
