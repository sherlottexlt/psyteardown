# 重叠启发式分支须显式解决冲突

## Status

accepted

候选同时匹配多个建议冲突的条件性启发式分支时，系统保留各分支的匹配条件、结果、证据、代价和未决变量，不按数量、具体性或版本新旧自动覆盖。由 DesignBrief 权衡和人工选择决定采用、并列验证或探索；缺少裁决依据时进入 `explore`。只有确定性 Approved Risk Rule 可按既有优先级压过启发式建议。
