# 选择执行者或模型变更默认切断支持继承

## Status

accepted

更换候选筛选执行者、生成模型或模型版本时，默认标记 `selection_executor_change` 并切断旧支持继承。只有候选池、筛选规则、排序依据、人工覆盖权限、随机性控制和可选范围冻结且经人工证明执行等价，才可在新 revision 中有限继承；最终选中相同候选不证明过程等价，无法验证时必须建立新选择链和前瞻性验证。
