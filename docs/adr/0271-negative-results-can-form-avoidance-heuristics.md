# 负向结果可形成条件性避免启发式

## Status

accepted

稳定、方法有效且达到确认性判据的负向结果，可以形成绑定用户、场景和变量范围的条件性避免启发式，但仍需两个独立 `supported`/更高支持单元和一次合格前瞻性检验。它只能以 `consider_avoiding` 或 `validation-only` 使用，不能自动成为 `must_not`、`blocked` 或 Approved Risk Rule；高风险禁止须另行审批。
