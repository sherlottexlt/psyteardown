# 显式处理设计候选中的证据冲突

## Status

accepted

当设计描述、规格、交互故事、渲染图或其他来源互相矛盾时，系统创建 Evidence Conflict，保存冲突声明、来源、适用候选版本、影响的 ReviewItem 和解决状态。规格/样机等高强度证据可以影响判断，但不能静默覆盖其他来源；无法解决时标记 unresolved，并降低相关结论强度。渲染图看不出某属性时记录 not_observable，不等于属性不存在。只有人工确认的解决结果才能进入下一轮设计提示。
