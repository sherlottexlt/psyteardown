# supported 与 replicated 使用不同证据门槛

## Status

accepted

一次预注册实验只有在主要指标判据满足、条件呈现可验证、分析运行可复现且经人工 Result Review 后，才能把结果推进到 `supported`。`replicated` 还要求新的参与者样本和新的 AnalysisRun 按相同或兼容协议得到一致结果；同一数据重算、模型重跑或单纯池化不构成独立复现。
