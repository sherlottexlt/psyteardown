# 新增可观测性不能追溯补足旧资格

## Status

accepted

历史关键字段缺少日志或可核验证据时，必须标记 `independence_coverage_gap`；后续新增的日志、监控或数据链只适用于未来运行或新 revision，不能倒灌证明旧实验的独立性。旧结果最多保留为 `limited_external_support` 或 `needs_reanalysis`，补足资格须按新协议重新验证。
