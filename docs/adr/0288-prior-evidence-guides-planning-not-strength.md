# 旧证据可指导规划但不提升新版本强度

## Status

accepted

既有结果可以用于新实验的样本量、效应范围、最小实际重要性、混淆变量、停止规则或事前冻结的 Bayesian 先验，并需进行预注册敏感性分析；但不能直接增加新版本 Outcome Evidence Strength。新版本的 `supported/replicated` 只能由新数据自身满足判据后产生，旧证据与新数据冲突时分别审阅。
