# V1 固定小规模候选批次

## Status

accepted

每个 DesignBrief 第一轮生成 5 个候选，人工选择 1–2 个方向，第二轮每个选定方向生成 3 个变体。每个候选约 4–6 个 ReviewItem，形成可人工审阅的批次。该规模用于验证事实确认、候选偏序、人工覆盖和变量修复追踪，不是生产环境的搜索上限。这样保留必要的设计发散，同时避免人工只看模型摘要；代价是 V1 不评估大规模候选搜索能力。
