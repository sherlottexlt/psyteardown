# 使用人工确认的结构化 Next Design Prompt

## Status

accepted

下一轮 AI 设计生成器只消费结构化的 `Next Design Prompt`：冻结的 DesignBrief 快照、人工选定的设计方向、确认的可操作修改和探索任务。完整评审报告、单一候选排名和未经确认的模型意见不能直接回灌。这样让设计生成器获得足够的上下文，又保留人工对反馈的责任和控制；代价是需要一个明确的确认界面或 CLI 步骤。
