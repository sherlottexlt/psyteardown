# 严格复现要求主要声明可计算复现

## Status

accepted

严格经验复现要求主要预注册指标、成功判据和事前锁定关键派生量在声明容差内重现，可标记 `analysis_reproducible_for_primary_claim`。次要指标无法重现必须单独降级；主要指标或判据不可重现时只能进入 `needs_reanalysis/inconclusive`，方向一致不能替代计算复现。
