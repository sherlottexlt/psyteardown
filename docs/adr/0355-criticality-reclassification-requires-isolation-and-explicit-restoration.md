# 执行关键性重分类须隔离并显式恢复

## Status

accepted

若实验后发现原标记为非关键的执行能力可能改变候选筛选结果，必须立即暂停受影响范围的新注入与资格累积，保留历史结果并标记 `qualification_at_risk` 或 `needs_reanalysis`。随后通过字段级影响分析隔离确未受影响的范围；修复或重分析不能自动恢复支持，必须建立新 revision 并经人工审阅显式恢复。
