# 评审反馈须经人工确认后回灌设计生成

## Status

accepted

psyteardown 可以自动生成设计评审草案，但只有经过人工确认、修改或明确采纳的反馈，才能形成 `Next Design Prompt` 并回灌给下一轮 AI 设计生成器。`blocked` 默认阻止候选继续循环，`revise` 可在人工确认后转为修改约束，`explore` 只能形成验证或探索任务。每轮保存简报快照、候选、评审、排序、人工选择和覆盖理由。这样避免设计模型和评审模型把未经验证的心理假设互相强化；代价是流程保留人工判断，自动化速度较慢。
