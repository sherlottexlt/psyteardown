# 跨项目参与者独立性不能默认成立

## Status

accepted

跨项目无法共享身份映射时，不得默认参与者独立；预注册必须声明可检测范围和限制，并使用研究专属 token、受控哈希校验或第三方匹配等最小化去重信号。确认重复者按同一参与者处理；无法确认且重复风险较高时标记 `participant_independence_uncertain`，按独立/非独立边界做敏感性分析，无法排除影响则不计入严格复现或启发式外部准入。
