# 信息先验不能单独制造复现

## Status

accepted

旧证据形成的信息 Bayesian 先验必须与新数据贡献分开，并在预注册中包含弱信息/怀疑性先验敏感性分析。只有不依赖旧证据推动的分析仍满足主要判据时，新实验才可获得 `supported`；仅信息先验下成功标记 `prior-dependent/inconclusive`，不能计入 `replicated` 或启发式独立支持。
