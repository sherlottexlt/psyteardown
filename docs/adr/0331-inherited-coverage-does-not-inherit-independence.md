# 继承前瞻覆盖不等于继承外部独立

## Status

accepted

`inherited_prospective_coverage` 只证明旧检验覆盖窄范围，不自动继承外部独立资格；旧检验的 `limited_independence`、`shared_infrastructure_dependency` 和 `prior-dependent` 限制必须保留。旧检验无合格外部/结果盲法资格时，窄范围 revision 仍需追加新检验；资格记录分开列出覆盖、执行独立性和推断限制。
