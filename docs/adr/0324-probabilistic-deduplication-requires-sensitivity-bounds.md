# 概率去重必须做边界敏感性分析

## Status

accepted

跨项目概率匹配不能按最可能匹配作单一判断，必须记录匹配概率、依据和不可判定范围，并按预注册的全部独立、可能重复者剔除和最保守重复等边界分析。只有所有合理边界下独立样本、主要判据和方向仍满足要求，才可计入严格 `replicated`；结论依赖匹配假设时维持 `participant_independence_uncertain`，确定性证据需新修订。
