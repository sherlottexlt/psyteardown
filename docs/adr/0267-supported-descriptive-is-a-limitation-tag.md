# supported_descriptive 是限制标签而非证据等级

## Status

accepted

`supported_descriptive` 不加入 `none → exploratory → observed → supported → replicated` 的 Outcome Evidence Strength 主序列，而作为 Result Review 的解释限制标签。它不能满足启发式支持、严格复现或范围扩张的确认性门槛，展示时必须同时显示 descriptive/unadjusted 限制。
