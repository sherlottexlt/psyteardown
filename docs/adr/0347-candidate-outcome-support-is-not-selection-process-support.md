# 候选结果支持不等于选择流程支持

## Status

accepted

多 seed 产生不稳定候选但冻结单一候选并完成合规实验时，结果可支持该候选的结果假设，但不能证明启发式选择流程稳定有效。分别记录 `candidate_outcome_support` 与 `selection_process_support`；从多个 seed 挑选最佳候选标记 `selection_enrichment`，选择流程需多 seed/多候选预注册对照和独立样本验证。
