# 设计资产和候选采用不可变版本链

## Status

accepted

DesignBrief、设计候选、输入资产和评审均以不可变版本保存，后续修改创建新的 revision，并通过 parent-child 关系连接。每个 ReviewItem 必须指向具体候选版本和证据版本；跨轮修复追踪不允许比较不同 brief 或无明确父子的候选。这样可以重建每一轮设计和反馈依据，避免历史结果被覆盖；代价是存储和界面需要处理版本树。
