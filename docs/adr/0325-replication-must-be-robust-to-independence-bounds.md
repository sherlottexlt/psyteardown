# 严格复现须对独立性边界稳健

## Status

accepted

任一合理参与者独立性边界使结果不满足 `replicated` 时，不得升级为 `replicated`；新实验自身可保留 `supported + replication_sensitivity` 并展示全部边界结果。该受限结果不能满足启发式外部独立准入或新资格累积，受影响范围暂停扩张，直至补足去重证据或完成独立性可确认的新实验。
