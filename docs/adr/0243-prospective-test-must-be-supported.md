# 前瞻性检验至少达到 supported 才具准入资格

## Status

accepted

启发式前瞻性检验至少达到 `supported` 才能作为批准依据，且不能替代此前两个独立支持单元。`observed`、`exploratory`、`needs_replication` 或 `inconclusive` 只能维持 Heuristic Candidate；明确冲突要求创建新 revision 并收窄范围或保留未决。
