# 监测窗口证据与确认性验证隔离

## Status

accepted

监测窗口候选和结果只能作为新预注册验证的设计、边界、方差和反例输入，并标记 `monitoring_evidence`，不能计入确认性样本或启发式支持。监测与验证候选/条件使用独立 revision；窗口结果改变变量范围、候选选择或判据时必须新建预注册 revision。监测参与者不能直接并入验证样本，重复参与按学习/重复任务规则处理。
