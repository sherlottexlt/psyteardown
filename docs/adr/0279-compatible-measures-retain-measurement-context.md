# 兼容测量仍须保留实施情境

## Status

accepted

经审阅可标记 `implementation_compatible` 的不同测量实施方式可以计入同一假设的严格复现，但必须保存 `measurement_context`，并限制启发式只适用于已声明的实施条件。`replicated` 不表示效应量跨实施方式可交换；新的实施方式须单独验证，跨实施合并须事前预注册和方法审阅。
